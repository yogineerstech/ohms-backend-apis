meta {
  name: Get Upcoming Surgeries (R2) copy
  type: http
  seq: 7
}

get {
  url: http://localhost:3000/api/v1/surgery/recommended-patients
  body: none
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should return upcoming surgeries for receptionist", function() {
    expect(res.getBody().success).to.equal(true);
    expect(res.getBody().data.count).to.be.a('number');
    expect(res.getBody().data.surgeries).to.be.an('array');
  });
}
